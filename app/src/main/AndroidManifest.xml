<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aliya.constraint">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="ConstraintLayout使用示例">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AlignSimpleActivity"
            android:label="对齐 - 示例" />
        <activity
            android:name=".RatioSimpleActivity"
            android:label="比例 - 示例" />
        <activity
            android:name=".ChainsSimpleActivity"
            android:label="链式 - 示例" />
        <activity
            android:name=".WeightSimpleActivity"
            android:label="weight - 示例" />
        <activity
            android:name=".GuideLinesSimpleActivity"
            android:label="基线 - 示例" />
    </application>

</manifest>